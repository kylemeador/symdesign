#!/bin/bash
#SBATCH --partition=long
#SBATCH --ntasks=1
#SBATCH --cpus-per-task=1  # starting one task through sbatch, 2 with python
##SBATCH --threads-per-core=2  # requires that cores contain dual threads (hyperthreading) to make use of second python thread. no specification on cassini
#SBATCH --mem-per-cpu=16000  # value requested is doubled as it is a hyperthreaded job
#SBATCH --no-requeue  # prevent failures from restarting automatically
